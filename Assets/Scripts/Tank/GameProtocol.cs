using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameProtocol 
{

    public enum Handler
    {
        POSITION = OnlineProtocol.Handler.GAME_PROTOCOL_START,
    }
}
